<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
     <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
        <a class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block mb-5" href="/logout">Logout</a>
    </div>

    <h5 class="mb-5">You can edit your post</h5>
    <form action="/update/<%= post._id %>" method="post">
        <textarea placeholder="What's on your mind?" class="w-1/3 resize-none bg-white text-zinc-800 p-4 px-3 border-2 border-green-900 outline-none rounded-md" name="content"><%= post.content %></textarea>
        <input class="px-4 py-3 bg-yellow-500 rounded-lg block md-2 w-40 text-black" type="submit" value="Update post">
    </form>
   
    </div>
</body>
</html>